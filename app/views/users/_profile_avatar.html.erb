<div data-controller="edit-profile" data-edit-profile-target="profile">
  <%= @user.first_name %>
  <%= cl_image_tag @user.photo.key %>
  <%= simple_form_for @user, url: user_update_avatar_path(@user), method: 'post', html: {data: { edit_profile_target: 'form', action: 'submit->edit-profile#updateAvatar' }} do |f| %>
    <%= f.input :photo, input_html: { data: {action: 'change->edit-profile#updateAvatar'.html_safe} }%>
    </div>
  <% end %>
  </div>